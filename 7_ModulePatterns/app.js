var stuff = require('./main')

// stuff(['rohityadav', 'ryu', 'sonali']);

// so now executing multiple methods use rstuff which is an objec along with the property name

stuff.counter(['rohityadav', 'ryu', 'sonali']);

stuff.adder(5,6);


stuff.adder(stuff.pi, 2);
